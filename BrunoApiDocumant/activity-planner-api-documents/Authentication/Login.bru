meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{ApiUrl}}Authentication/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "ananasor",
    "password": "123456Aa"
  }
}

script:post-response {
  bru.setEnvVar("Token",res.body.accessToken);
  bru.setEnvVar("TokenRefresh", res.body.refreshToken);
}
